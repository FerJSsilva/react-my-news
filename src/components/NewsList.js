import React from 'react';

const NewsList = () => (
  <div className="news-list">
    <div className="news-control">
      <button type="button" className="show-more">Mostrar mais</button>
    </div>
  </div>
);

export default NewsList;
